name: Update Repository

on:
  workflow_dispatch:
    inputs:
      versions:
        description: 'Packages to update (e.g., "keystone-cli" or "keystone-cli:0.1.9"). Leave empty for all packages.'
        required: false
        type: string
  repository_dispatch:
    types: [release-published]

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout apt repository
        uses: actions/checkout@v4

      - name: Install yq
        run: sudo snap install yq

      - name: Build version arguments
        id: args
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -n "${{ inputs.versions }}" ]; then
            # Use workflow_dispatch input
            echo "versions=${{ inputs.versions }}" >> "$GITHUB_OUTPUT"
          elif [ -n "${{ github.event.client_payload.versions }}" ]; then
            # Use repository_dispatch payload
            echo "versions=${{ github.event.client_payload.versions }}" >> "$GITHUB_OUTPUT"
          else
            # Fetch latest for all packages
            echo "versions=" >> "$GITHUB_OUTPUT"
          fi

      - name: Update repository
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ./scripts/update-repo.sh ${{ steps.args.outputs.versions }}

      - name: Import GPG key
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import
          FINGERPRINT=$(gpg --list-secret-keys --with-colons | grep fpr | head -1 | cut -d: -f10)
          echo "${FINGERPRINT}:6:" | gpg --import-ownertrust

      - name: Sign Release file
        env:
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: ./scripts/sign-release.sh

      - name: Remove .gitkeep files
        run: find dists -name ".gitkeep" -delete

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add dists/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            # Build commit message from updated packages
            PACKAGES=$(yq -r '.packages[].name' packages.yml | tr '\n' ' ')
            git commit -m "Update repository for: ${PACKAGES}"
            git push
          fi

